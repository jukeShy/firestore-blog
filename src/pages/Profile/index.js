import React from 'react';
import { Default, Section } from '~/layouts';

const Profile = () => {
  return (
    <Default>
      <Section>
        <h1>Profile</h1>
      </Section>
    </Default>
  );
};

export default Profile;
